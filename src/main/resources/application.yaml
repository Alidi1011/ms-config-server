server:
  port: 8081
spring:
  application:
    name:
      ms-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Alidi1011/project_configurations.git
eureka:
  client:
    region: default
    serviceUrl:
      defaultZone: https://bootcamp-ms-eureka-server-g3.azurewebsites.net/eureka/
    registryFetchIntervalSeconds: 5
